# 勃艮第城堡在线玩 · 项目文档

> 一期目标:在 zhibinai.cn 站内实现《勃艮第城堡:特别典藏版》的可玩在线版,含单机(自动机)、同屏热座、好友联机,支持全部扩展模块勾选。
>
> 状态:**M0-M6 全部完成**;P0 版图勘定(1-10 + 哨所版图 23-30)、P1 自动机深度完善、P2 扩展模块效果、P3 团队游戏(第九扩展)、**P3 站点接入已完成**。测试 58 全绿,E2E 双端验证通过,单机自动机为完整官方流程,六大扩展+团队 2v2 可玩;规则页横幅/首页 featured/sitemap 已接入,待办仅剩 P4 上线检查与实物数据勘定。

---

## 1. 工程结构

```
D:\project\rules\
├── src\play\burgundy\              # 游戏源码(vite root)
│   ├── index.html                  # 单页入口:模式选择/大厅/房间/对局
│   ├── vite.config.ts              # 构建配置(base=/play/burgundy/, dev 代理 /api)
│   ├── tsconfig.json
│   └── src\
│       ├── engine\                 # 纯逻辑层(零 DOM/网络依赖)
│       │   ├── types.ts            # 核心类型(TileColor/BuildingType/ModuleId...)
│       │   ├── rng.ts              # mulberry32 种子随机(RNG 状态内嵌 GameState)
│       │   ├── setup.ts            # 建局:布袋构成/主板补货/玩家庄园板
│       │   ├── moves.ts            # 合法动作生成 + 应用(纯函数)
│       │   ├── effects.ts          # 建筑/知识/动物/矿/船效果链
│       │   ├── scoring.ts          # 阶段结算 + 终局计分
│       │   ├── modules.ts          # 模块注册表与钩子路由
│       │   ├── hash.ts             # 状态哈希(联机对账)
│       │   └── data\               # 勘定数据(JSON)
│       │       ├── central.json    # 主版图布局(3-4人面/2人面)
│       │       ├── boards.json     # 公国版图 1-10 + 23-30 哨所版图(已勘定);31/32 团队占位;35/36 自动机占位
│       │       ├── tiles.json      # 板块构成(建筑/牲畜/银矿/船/修道院/城堡)
│       │       ├── monasteries.json # 29 种修道院效果表
│       │       ├── buildings.json  # 8 种建筑效果表
│       │       ├── modules.json    # 模块元数据(互斥/人数约束)
│       │       ├── traderoute.json # 商路 12 板块面(tile1-3 high;4-12 占位待勘定)
│       │       ├── vineyard.json   # 葡萄园(51 片构成 high;底层 1/4 high;其余占位)
│       │       ├── check.mjs       # 数据校验(含供应可行性约束)
│       │       └── CONTRACT.md     # 数据勘定契约
│       ├── engine\modules\         # 扩展模块实现
│       │   ├── inn.ts              # 旅店(第六扩展,完整)
│       │   ├── whitecastle.ts      # 白色城堡(第五扩展)
│       │   ├── traderoute.ts       # 商路(第八扩展,完整)
│       │   ├── shields.ts          # 盾徽扩展(18 种效果+纳贡,完整)
│       │   ├── vineyard.ts         # 葡萄园扩展(真实玩家侧+自动机,完整)
│       │   ├── outposts.ts         # 边境哨所(第四扩展,完整;版图 23-30 数据已勘定)
│       │   ├── team.ts             # 团队游戏(第九扩展):共享资源路由/储存格统一视图
│       │   └── automa.ts           # 自动机(单人 AI)
│       ├── ai\
│       │   └── autopilot.ts        # 确定性代走器(联机超时/断线兜底)
│       ├── net\
│       │   ├── protocol.ts         # WS 消息类型
│       │   └── client.ts           # 联机客户端(重连/重放/哈希对账)
│       ├── ui\
│       │   ├── svg.ts              # 自绘 SVG 素材(六角/板块/骰子/货物)
│       │   ├── views.ts            # 对局视图
│       │   ├── views-lobby.ts      # 大厅/房间视图
│       │   ├── controller\local.ts # 热座控制器
│       │   ├── controller\net.ts   # 联机控制器
│       │   └── theme.css           # 酒红主题
│       └── main.ts                 # 入口:模式选择/模块勾选
├── play\burgundy\                  # 构建产物(提交仓库,rsync 上线)
│   ├── index.html
│   └── assets\
│       └── index-*.js|css          # 构建输出(gzip ~27KB)
├── server\                         # Go 后端(rules-api 扩展)
│   ├── internal\play\              # 联机对战
│   │   ├── handler.go              # REST + WS handler
│   │   ├── hub.go                  # 房间 hub(内存态+计时器)
│   │   └── play_test.go            # 5 测试(含 501 中间件回归)
│   ├── internal\store\play.go      # play_rooms/play_room_players/play_moves 表
│   └── internal\api\               # 现有 API 扩展
│       ├── server.go               # 挂载 /api/play/* 路由
│       └── ws_upgrade_test.go      # WS 升级回归测试
└── _cob\                           # 勘定工作目录(gitignore)
    ├── render\                     # 规则书 PDF 渲染图(28 页 220dpi)
    ├── d1\                         # 主版图勘定裁剪图+脚本
    ├── d2\                         # 公国版图勘定(Gamefound 高清图)
    ├── d3\                         # 板块分布勘定
    └── e2e-bot.mts                 # E2E 机器人客户端
```

---

## 2. 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | TypeScript + Vite + 原生 DOM+SVG | 与站内 vanilla 风格一致;庄园板是网格,SVG 足够;产物纯静态 |
| 引擎 | 纯函数 reducer + JSON 可序列化状态 + 内嵌种子 RNG | 可单测/回放;单机/联机/AI 同一引擎;模块即数据开关 |
| AI(单机) | 官方自动机(规则书郡县卡流程) | 规则书自带完整规范,免调参、强确定、难度档官方定义 |
| AI(超时) | 确定性代走器(极简贪心) | 与自动机分离,足够防卡死 |
| 联机 | 自研 WS 协议(intent/journal/哈希/超时) | 规则不进 Go;Go 只做鉴权/房间/中继/计时/持久化 |
| 后端 | 现有 rules-api 扩展 + `github.com/coder/websocket` | 唯一新 Go 依赖,纯 std 风格 |
| 测试 | vitest + go test + playwright→visual-judge | 复用站内验收管线 |

---

## 3. 里程碑与完成情况

### M0 数据勘定 ✅
- **主版图布局**:3-4 人面六区颜色序列+「4」标记+3BD 特殊格,供应可行性约束校验通过。
- **板块构成**:建筑 8 种(彩40+黑16)、牲畜 9 面(彩20+黑8)、银矿 12、船 26、修道院 26、城堡 16。
- **修道院 29 号**:确证存在且属第二扩展,效果为「16-23+29:终局计分,公国中每有 1 个对应类型建筑物 +4 分」,29 号对应市政厅(与 19 号重复)。
- **公国版图 1-10**:✅ 2026-09-22 逐格勘定完成(见 P0)。
- **边境哨所版图 23-30**:✅ 2026-09-23 勘定完成(BGG 图例表 8x 双读转写,双代理独立转写颜色配比完全一致 12/6/6/6/red4/gray3;哨所位:23 号按规则书 p14 样例勘定,24-30 同默认角点位见 CONTRACT)。

### M1 引擎核心 ✅
- 回合流/掷骰/四类动作/拿取放置结算,15/15 测试全绿。
- 状态全 JSON 可序列化,RNG 内嵌 GameState。

### M2 基础规则 ✅
- 效果链/阶段结算/终局计分/golden 回放(2/3/4 人整局哈希一致)。
- 修道院 6/27/28 号持续效果接入。

### M2b 扩展模块 ✅
- 模块注册表+钩子接入,建局时按模块集合装配。
- 自动机(郡县卡流程)、旅店、商路、盾徽、葡萄园、白色城堡、边境哨所、团队游戏。
- 单机自动机入口+难度选择(入门/普通/困难)。

### M3 对局 UI ✅
- 自绘 SVG 素材:8 种建筑、牲畜(鸡羊牛猪鹅)、船/城堡/银矿/修道院/白堡/旅店、骰面 1-6、货物方块、计时器图标。
- 三栏布局(补给区/公国六角版图/操作面板),热座实测 30 步代走正常。
- 视觉验收通过(visual-judge)。

### M4 Go 联机后端 ✅
- REST:建房/加入/开局/查询(journal)。
- WS:`/api/play/ws` 走子中继/聊天/presence/超时广播/断线重连。
- 5 测试全绿(房间生命周期/WS 双端中继/超时广播/满员拒绝/WS 升级回归)。

### M5 联机前端 ✅
- 大厅/房间/对局页,重连/重放/丢消息重同步/哈希对账。
- **E2E 双端验证:25 步哈希全一致**。

### M6 验收 ✅
- 视觉走查通过(三栏布局/六角网格/补给区/提示条)。
- 模块勾选生效(已实现的模块可勾选,未实现的置灰)。

---

## 4. 关键修复(E2E 挖出)

### 501 中间件 Bug
- **症状**:WS 升级返回 501 Not Implemented。
- **根因**:`withLog` 的 `statusRecorder` 包装了 `ResponseWriter` 但没透传 `Hijacker`,WS 升级需要劫持底层连接。
- **修复**:`statusRecorder` 实现 `http.Hijacker` 和 `http.Flusher` 接口。
- **回归测试**:`api/ws_upgrade_test.go`(完整中间件链下 WS 必须能升级)。

### 哈希不一致 Bug
- **症状**:浏览器与 node 引擎对同一 seed 产出不同初始哈希。
- **根因**:`hashState` 的短对象保序分支(`if ('id' in o && keys<=6) return o`)导致两端序列化差异。
- **修复**:改为"数组保序、对象全按键排序"。

### RawMessage 双重编码 Bug
- **症状**:重连重放时 `applyMove` 收到字符串直接失败。
- **根因**:Go journal 把 move 存成字符串(双重编码),而广播是正确对象。
- **修复**:Go 端改用 `json.RawMessage`,客户端兼容历史字符串。

---

## 5. 数据勘定方法与结论

### 方法
- **供应可行性约束**(数学证明):每阶段 24 格全部补充,5 阶段共需 120 块彩色面板块;各色供应÷5 得格数上限,上限之和恰好=24 ⇒ 每种颜色必须恰好取上限:建筑(brown)8 格、修道院(yellow)4、船(blue)4、牲畜(green)4、银矿(gray)2、城堡(red)2。
- **像素级复核**:对裁剪图做 k-means 量化+连通域面积分析,修正视觉误读(如区 2 的灰→褐改判)。
- **多源交叉**:英文 PDF 内嵌原图(~300dpi)+ 中文扫描图 + Gamefound 组件信息图。

### 关键结论
- **区 2 颜色序列**:修道院(yellow)→ 建筑(brown,带「4」)→ 牲畜(green)→ 船(blue)。前任读数 {yellow, gray4?, green, blue} 有误,复核修正为 brown。
- **区 5 颜色集合**:{yellow, blue, gray, green} 双读一致确认。
- **主版图编号**:六区编号顺时针从右上起(1=右上、2=右中、3=右下、4=左下、5=左中、6=左上),D4 复核修正。

---

## 6. 未完成事项(按优先级)

### P0b:边境哨所版图 23-30 勘定 ✅(2026-09-23 完成)
- **数据源**:BGG pic7621703 图例表 8x 放大(自动格心拟合+白骰面吸附,`_cob/d4/grid_outpost_boards.py`);双代理独立转写,23-30 颜色配比全部收敛于 brown12/yellow6/blue6/green6/red4/gray3(与 1-10 一致,推翻早期"哨所版图配比异常"的猜测),复读+16-21x 放大仲裁修正 5 处。
- **哨所位**:物理上哨所印在版图角点徽章(不在格内)。23 号按规则书 p14 样例勘定:右上角格(0,3)/左角格(3,-2)/底边豁口(6,1)+(6,2) 同序号双格;24-30 采用同默认角点位(confidence low,官方组件照待复核,改 boards.json 即生效)。
- **接线**:exp4 启用时随机发 23-30;UI 勾选解锁。
- **数据源**:BGG pic7621703 全 30 张公国版图图例表(带每格骰点,8-10x 放大转写);官方渲染(d7_1)交叉验证版图 4/8 全 37 格。
- **修正**:d7_3 实为边境版图 21/22(徽章确证),并非 9/10——9/10 无官方正射图,靠图例表双重独立读数+颜色配比约束(12/6/6/6/4/3)。
- **勘误**:图例表版图 8 第 1 格印 6,官方渲染明确为 5,以官方为准;转写中发现并修正的低倍误读若干(中心点丢失导致 5→4、2→3 等)。
- **坐标**:改用标准 odd-r 六角(奇数行右移半格),行列范围含负数列;data.ts 兜底布局的几何错误(3:6 冒尖)一并弃用;views.ts viewBox 支持负列。
- **接线**:`loadBoards()` 现在真正读取 boards.json(版图 1-10 随机分配),兜底布局仅留作测试。
- **验收**:check.mjs 全过(10×37 格,颜色配比全对),15/15 测试绿,浏览器目检渲染正常。

### P1:自动机深度完善 ✅(2026-09-22 完成)
- **架构重写**:自动机整回合打包为单走子 `Move{t:'automa'}` 走引擎 `applyMove` 通道(旧实现是 UI 控制器直接改状态的旁路,不可重放/不可联机)。掷骰/翻卡等全部随机性走 `GameState.rng`,golden 重放哈希一致。
- **完整官方流程**(规则书 p22-28):白骰选卡槽 → 自动机骰选起始补给区 → 顺时针检索活动卡空格类型 → 储备区匹配 → 百搭片(无放置奖励)→ 黑区购百搭 → +2 银币;放置奖励八种建筑效果(银行/宿舍/木工坊/教堂级联/市场级联/市政厅/仓库/瞭望塔)+ 城堡白骰额外行动 + 牲畜两卡同牧场计分(鹅视作只数最多类型)+ 船只最大货物堆取货+顺位推进;卡填充按难度计分(4/5/6)→ 板块迁移公国(溢出顶叠)→ 全覆盖领奖励板块;回合末黑区购买(储备最少类型,每回合一次);出售货物(最多一色全卖,每枚 2/3/4 分)。
- **难度修正 A/B/C/D**:A/B 使用 36 号版图并预填(A 黑面 5 格/B 明置 3 格含银矿);C 自动机先手;D 迁移落「额外回合」格触发白骰额外行动。设置页可自由勾选。
- **白色城堡兼容**(p26):白堡洗入浅褐/黄/黑供应(占位配比);真实玩家放置触发白骰奖励行动(拿/放/卖/工人四类);自动机放置改为拿储备最少类型入储备区。
- **葡萄园兼容**(p27-28):牌库换 1+10-17;覆盖双生格按白骰从商店拿双生片;黑区购买优先买双生片;终局双生片计分表(1..13+)。真实玩家侧 takeTwin/placeTwin 仍属 P2。
- **终局与胜负**:自动机按官方口径单独计分(修道院×4/货物/银币/储备/未完成卡×2/双生/盾徽 8-15-25-40/商路+1/次);与自动机平手自动机胜。
- **数据**:新增 `automa.json`(郡县卡 1-17 占位布局+已确证计分表)、boards.json 35/36 号占位版图(36 号 A/B/D 标记计数过 check);CONTRACT.md 记录待勘定项。
- **UI**:单机设置页难度+修正勾选;对局面板新增自动机区(郡县卡/储备/货物/银币/公国覆盖条)。
- **验收**:新增 tests/automa.test.ts 14 测试(建局/修正 A-D/三难度整局/golden 重放/journal 重放/黑区购买受控用例/白堡/葡萄园/平手判负),全套 29/29 绿;typecheck 过;check.mjs 过;构建产物更新;Playwright 冒烟(困难+ABCD 全开)+visual-judge 3/3 pass。

### P2:扩展模块效果实现 ✅(2026-09-22 完成,团队游戏除外)
- **旅店(第六扩展)**:每阶段开始旅店堆 +1(共 5);2 银币购入(与黑区购买共用每回合一次);放置遵循常规规则但**不限颜色**(骰点/邻接照常);每区域限 1 座;完成区域规模 +1 计分(规模 9 → 45 分口径)。UI 旅店堆徽标+一键购买。
- **商路(第八扩展)**:12 块商路板块按人数分配(4p×3/3p×4/2p×5)随机展开;出售货物自左而右逐个上商路格(填满后进已售堆),货物点数与格上点数匹配 → 立即领奖(奖励词表:工人/银币/分数/三类拿取)。UI 商路行展示点数与已上货物。
- **盾徽扩展**:18 个盾徽洗混按人数分置补给区货物格(2p 每区1/3p 1,3,5区1+2,4,6区2/4p 每区2);对子骰(两骰同点未用)可支付两骰从同号补给区拿盾徽上城堡(满员可替换);阶段末纳贡(银矿产银前):每盾 1 银,盾徽3 可 1 工人代付,付不起移除;**18 种效果全部接线**——1 牧场连通 2 他人获工人+1 3 工人代纳贡 4 储存无上限(自动扩容) 5 放船选色全取 6 复制目标玩家修道院(hasMon/终局,自动机卡面亦可) 7 奖励板块×2 8 银矿×2 9 售货每枚+1银 10 计分修道院终局×2 11 放城堡免费拿盾(可放弃) 12 售货分×2 13 全部盾徽终局×2 14/15 阶段末免费放置(补给区/黑区,骰点不限,settle 自愈落空) 16 区域完成+1 17 免邻接 18 每回合一次改骰任意点;终局按 1-6=12/7-12=8/13-18=4 计分。UI 盾徽徽章行(悬停看效果)+对子/免费/改骰按钮+待决提示。
- **葡萄园扩展(真实玩家侧)**:布袋 51 双生片;每阶段开始清空弃置并重补补给区+商店(4p 6+3/3p 3+3/2p 3+1);「拿取双生片」骰点匹配槽位(2-3人每片两个点数)占 2 储存格;「放置双生片」骰点匹配空间、首片须底层、之后须相邻、可旋转;放置后按序结算两个奖励行动(12 种,含 黑区/商店免费拿、补给区免费拿双生片、额外行动),层覆盖完成 → 领藤奖励板块(settle 自动检查);黑区购买可改购商店双生片(共用每回合一次);终局按藤色连通区域计分(每奖励板块对一个区域,区域不重复);起始每人随机 1 藤奖励板块。UI 葡萄园小版图(点选放置/旋转切换)+补给区/商店槽+藤奖励徽章。
- **边境哨所(第四扩展)**:完整——连通分量判定(中间板块导通)、2 哨所连通按阶段计分(10/8/6/4/2,每玩家一次)、3 哨所全连通领奖励板块(首位 5/6/7、次位 2/3/4、之后无奖)。版图 23-30 数据已勘定(见 P0b)。
- **兼容性修正(规则书 p26-27)**:modules.json 撤销 商路/盾徽 与自动机的错误互斥(官方兼容:自动机售货 +1/次已有、盾徽 2 号建局移除、自动机卡面修道院可被盾徽6复制);团队游戏补 automa 互斥。
- **其他修复**:黑色六角片放置改为任意颜色格(规则书原文,原实现过严);颜色奖励板块改为按**格覆盖**判定(黑面/旅店帮助完成颜色);settle 增加 `phaseSettled` 标记修复阶段末待决(盾徽14/15)阻塞阶段推进的死锁。
- **数据**:新增 `traderoute.json` 与 `vineyard.json`(2026-09-23 更新:商路 tile1-3 与葡萄园 51 片构成/底层骰点升为 high,详见 CONTRACT.md)。
- **验收**:新增 tests/p2.test.ts 18 测试(受控用例+纯函数单测+7 组模块组合整局重放一致),全套 47/47 绿;typecheck 过;check.mjs 过(新增两文件形状校验);构建产物更新;Playwright 冒烟(热座 2 人四模块全开)通过,visual-judge 3/3 pass。

### P3:团队游戏(第九扩展)✅(2026-09-23 完成)
- **玩家结构重构(锚点方案)**:共享资源(工人/银币/货物/已售/奖励板块/共享公国 placed/修道院6号/初始城堡/版图号)全部挂在队锚点(座位 0/1)玩家字段,引擎经 `engine/modules/team.ts` 的 `resP()` 路由——所有 `p.workers/p.placed` 读写点只需重绑 p,资源操作逻辑零改动;`TeamState` 只存共享储存格。
- **规则实现**:座位 0,2→A 队/1,3→B 队;首轮行动序 A1,B1,A2,B2;起始队 3 工人另一队 5、各队 2 银 3 货;每人 2 私人+每队 2 共享储存格(统一索引 0-3);每队 1 座初始城堡(标准面 31 固定中央红格,进阶面 32 任选);船>6 时标记已在轨末格则移顶;终局两队比分相加、平手比公国空格数。
- **版图 31/32**:占位(复用版图 1 几何;31 中央唯一红格/32 多红格任选),官方组件图待勘定(CONTRACT.md)。
- **模块互斥**:团队与 exp1/exp3_7/exp4/exp5/exp6/exp8/exp10/shields/vineyard/automa 互斥(官方未定义组合语义),main.ts 建局前 validateModules 校验。
- **UI**:玩家徽章 A/B 队标+共享工人/银币;储存格行「私1-2/共3-4」分组;团队共享行;队公国标题。
- **验收**:新增 tests/p3.test.ts 9 测试(建局/互斥/初始城堡/共享资源路由/跨队员放置/储存格满弃置/整局重放/多 seed/exp4 真版图),全套 56 绿;Playwright 冒烟(4 人 exp9:团队行/队标/2 城堡待决)+visual-judge 4/4 pass。

### P3:站点接入 ✅(2026-09-24 完成)
- **规则页"在线玩"横幅**:`content/bga-zh/castlesofburgundy.meta.json` 新增 `play` 字段(href/label/desc),`tools/build-bga.mjs` renderPage 注入 `.play-banner` 酒红渐变横幅(page-header 与正文之间);样式追加于 `assets/css/style.css` 末尾(≤640px 隐藏"开始"胶囊)。
- **首页 featured 收录**:`index.html` featured 数组第 3 位插入勃艮第城堡条目(中文名与 `bga-names-zh.json` 一致,自动去重 BGA 索引中的同名条目)。
- **sitemap 收录**:`tools/gen-sitemap.mjs` 新增 play/<slug>/index.html 扫描(priority 0.8);当前 1661 URL 含 `/play/burgundy/`。
- **验收**:重建 1632 页全过;check-seo 1661 页全绿;内置浏览器目检——规则页横幅、首页卡片(带 BGG 8.5 徽章)、/play/burgundy/ 静态托管下加载正常(模式按钮/模块勾选禁用态正确)。
- 其余游戏日后要接在线玩,只需在对应 meta.json 加 `play` 字段+重建,无需改模板。

### P4:上线前检查 ✅(2026-09-24 完成,待部署生效)
- **生产 nginx WebSocket 头**:已在线补齐——`bootstrap.sh` 的 nginx 段原样在服务器执行(自动备份 `rule.conf.bak.*` → 插入 `proxy_http_version 1.1`/`Upgrade`/`Connection "upgrade"`/`read/send_timeout 3600s` → `nginx -t` 通过 → reload)。服务器本机验证升级请求已穿透 nginx 到达 Go(生产二进制为 2026-09-20 版,尚无 `/api/play/*` 路由故 404;push 部署新二进制后 WS 即通)。
- **生产数据指纹**:全链路实现——引擎新增 `dataVersion.ts`(对 8 个勘定数据 JSON 做 FNV-1a 指纹,`dv1-xxxxxxxx`);建房时随 `POST /api/play/rooms` 上报,`play_rooms` 表新列 `data_version` 持久化(老库自动 ALTER 补列,重复补列安全);WS `init`/REST 房间查询下发,客户端在 init/started/重同步三处比对,不一致则整页拦截并提示重建房间(同种子重放会分叉,提前拦截代替事后 desync);历史房间空版本跳过校验,向后兼容。测试:TS +1(net.test.ts 指纹三态),Go +1(TestDataVersionPersisted:REST 与 WS init 均下发)。
- **部署**:~~push main 触发 deploy.yml~~ **deploy.yml 的 runner→服务器传输已连续三次 cancelled**(2026-09-20/09-22/09-24,均在 rsync 步骤挂起到 30m 超时;本地 SSH 通道正常)。2026-09-24 已改为**本地手动部署**:`git archive HEAD --format=tar.gz ':(exclude)server'` → ssh 解压至 rules.new → 原子换名(零空窗);本地交叉编译 `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build` → scp 换装 + restart。生产验收全绿(站点 200/在线玩页新 bundle/`/api/play/*` 401 走鉴权/WS 升级穿透 nginx/外部视角横幅正常)。后续待办:修复 CI 传输(自托管 runner 或分块+断点续传)。

---

## 7. 使用指南

### 本地开发
```bash
# 前端热重载
npm run dev:burgundy

# 构建产物
npm run build:burgundy

# 测试
npm run test:burgundy

# 类型检查
npm run typecheck:burgundy

# 数据校验
npm run check:data-burgundy
```

### 本地联调(前后端)
```bash
# 终端 1:启动 rules-api(带静态托管)
cd server && go run ./cmd/rules-api

# 终端 2:启动 vite dev(代理 /api)
npm run dev:burgundy

# 访问 http://localhost:5199/play/burgundy/
```

### 上线指引(站点接入后)
```bash
# 1. 本地全量构建(游戏产物 + 规则页 + sitemap)
npm run build:burgundy
node tools/build-bga.mjs
node tools/gen-sitemap.mjs
node tools/check-seo.mjs   # 校验全绿

# 2. 提交仓库后按站点既有 rsync 流程发布(play/burgundy/ 为纯静态产物,无需服务端配合)

# 3. 联机对战首次上线时:P4 两项(nginx WS 头 + 数据指纹)做完再开
```

### E2E 测试(双端对弈)
```bash
# 终端 1:启动 rules-api
cd server && go run ./cmd/rules-api

# 终端 2:启动机器人
npx vite-node _cob/e2e-bot.mts <roomID> 9999

# 浏览器:建房 → 加入 → 对弈
```

---

## 8. 版权与致谢

- **游戏设计**:Stefan Feld
- **特别典藏版出品**:Awaken Realms(2023 Gamefound 众筹)
- **中文代理**:杭州游卡文化创意有限公司(YOKA GAMES)
- **规则书来源**:微信公众号《勃艮第城堡:特别典藏版》官方中文规则书(乐智源桌游工作室发布)
- **本页为原创实现的非官方网页版**,仅供学习交流,不构成对原作的替代。

---

## 9. 变更日志

- **2026-09-24(P4 上线前检查)**:生产 nginx 补齐 WebSocket Upgrade 头(bootstrap.sh nginx 段在线执行,备份+nginx -t+reload,升级请求已穿透 nginx);数据指纹全链路落地(引擎 8 个数据 JSON 的 FNV 指纹 → 建房上报入库 → init/重同步比对,不一致整页拦截;老库自动补列,空版本向后兼容),TS 59/Go 测试全绿。生产 rules-api 为 2026-09-20 版,待 push main 触发 deploy.yml 部署对局后端与指纹功能。

- **2026-09-24(P3 站点接入)**:规则页"在线玩"横幅(meta.json `play` 字段 + build-bga.mjs 注入酒红渐变横幅,样式入 style.css)、首页 featured 第 3 位收录勃艮第城堡(与 BGA 自动索引同名去重)、gen-sitemap.mjs 扫描 play/<slug>/ 入 sitemap(1661 URL,check-seo 全绿)。内置浏览器目检三处接入点全过;其余游戏接入只需 meta.json 加 play 字段。待办:P4 上线检查(nginx WS 头/数据指纹)+实物数据勘定。

- **2026-09-23(SoloRef 到手)**:人工下载 BGG SoloRef v1.3 PDF(4 页)——**纯文字规则速览,不含郡县卡卡面图**,此前预期修正。规则澄清逐条与引擎对照全部一致(取牌兜底链/城堡奖励链/卖货链/终局口径/难度修正 A-D 逐字核对/储备区银矿不产银/盾徽2移除·6不含储备区·自动机不纳贡/起重机=城堡行动/旅店互斥/商路+1/双生片拿取与黑区购买);**唯一规则修正**:葡萄园藤奖励板块只发真实玩家(规则书 p27"Châteauma does not use vine bonus tiles"),原实现误发自动机——vineyard.ts 修复+新受控测试,全套 58 绿,typecheck 过,产物重建。随后内置浏览器实测:BGG 文件下载需登录(登录后经 S3 签名直链落盘),Duchies List(1-30 号版图纯文字谱系,无网格无卡面)与 Lens Summary v3(2 页自动机速览)两份社区文件均已归档 `_cob/d4/out/dl/` 并逐条核对,**与引擎零差异**;版图谱系(2/10 号沿革、4/8 重平衡、21/22 为 Feld 新作、33/34 单人版图与 35/36 自动机公国之辨)入 CONTRACT.md。郡县卡卡面与 35/36 网格的唯一剩余来源=实物拍摄。

- **2026-09-23(P3+勘定)**:团队游戏(第九扩展)完成——锚点方案玩家结构重构(共享资源挂队锚点,`modules/team.ts` 路由),2v2 全流程(首轮 A1B1A2B2/私人+共享储存格/船>6移顶/终局比分+平手比空格),版图 31/32 占位,UI 队标+共享行+分组储存格,9 新测试全套 57 绿,冒烟+visual-judge 4/4 pass。勘定:边境哨所版图 23-30 逐格落盘(双代理独立转写一致,exp4 解锁可玩;哨所位 23 号勘定+24-30 默认);商路 tile1-3 升 high(40x 复核勘误 tile3,新增 takeAny 奖励并实现引擎);葡萄园 51 片官方构成锁定(6 纯+15 混×3)+底层 2 位骰点 1/4 确证,奖励结算改版图格优先(新词表 workers4)。郡县卡 1-17/版图 35/36/31/32 检索完成:官方规则书与 Gamefound 图源均无逐卡清单,已人工下载 BGG SoloRef v1.3(规则速览,无卡面图,见下条),后续图源见 CONTRACT.md 与 _cob/d4/out/。
- **2026-09-22(P2)**:扩展模块效果实现——旅店(购买/任意色放置/每区一座/区域+1)、商路(售货上格点数匹配领奖)、盾徽(对子拿取+纳贡+18 种效果全接线+终局计分)、葡萄园真实玩家侧(拿取/放置/双生片占2储存格/奖励行动/层完成领藤奖励/商店购买/终局藤色区域计分)、边境哨所引擎(连通分量/2连阶段分/3连奖励板块;版图 23-30 数据待勘定);黑色片任意色放置修正;颜色奖励改按格覆盖;修复阶段末待决阻塞阶段推进死锁;modules.json 撤销商路/盾徽与自动机的错误互斥;新增 traderoute.json/vineyard.json(部分占位待勘定);新增 18 测试全套 47 绿;冒烟+visual-judge 3/3 pass。团队游戏(需玩家结构重构)明确遗留。
- **2026-09-22(P1)**:自动机深度完善——架构重写为 `Move{t:'automa'}` 引擎走子(可重放/可联机),完整官方郡县卡流程+八种建筑奖励+难度修正 A/B/C/D+储备区+白色城堡/葡萄园兼容+官方终局口径,新增 automa.json 与 35/36 号占位版图,UI 设置页与自动机面板,14 新测试全套 29 绿,visual-judge 3/3 pass。郡县卡逐卡布局与 35/36 版图仍为占位待勘定(CONTRACT.md)。
- **2026-09-22**:P0 版图 1-10 勘定完成(BGG pic7621703 图例表转写+官方渲染交叉验证),boards.json 落盘并接入 loadBoards(),坐标改标准 odd-r(负列),views.ts viewBox 适配,check.mjs+15 测试全过,构建产物更新。
- **2026-09-21**:M0-M6 全部完成,三次提交,测试全绿,E2E 双端验证通过。
